import java.util.Random; //standin

public class ShowMonster{

  Random generator = new Random();
  int randomIndex = generator.nextInt(monsterList.length);
  return monsterList[randomIndex];

  public static boolean chooseMonster(int difficulty){
    
        }
